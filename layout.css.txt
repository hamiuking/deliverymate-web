/* Page container */
.container {
  max-width: 900px;
  margin: 0 auto;
  padding: 20px;
}

/* Top navigation bar */
.topbar {
  background: rgba(255,255,255,.8);
  backdrop-filter: blur(10px);
  border-bottom: 1px solid var(--border);
  padding: 12px 20px;
  position: sticky;
  top: 0;
  z-index: 50;
}

/* Navigation links */
.nav {
  display: flex;
  gap: 12px;
}

.nav a {
  text-decoration: none;
  color: var(--primary);
  padding: 6px 10px;
  border-radius: 8px;
  background: rgba(255,255,255,.6);
  border: 1px solid var(--border);
  font-size: 14px;
}

.nav a.active {
  background: var(--primary);
  color: #fff;
}

/* Grid (used in admin dashboard) */
.grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
  gap: 12px;
}